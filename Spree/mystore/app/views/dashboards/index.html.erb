<!-- <h1>Listing dashboards</h1> -->

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <center>
<%= image_tag @graph %>
<br>
<%= image_tag @graph2 %>
<br>
<%= image_tag @graph3 %>
<br>
<%= image_tag @graph4 %>
<br>
<%= image_tag @graph5 %>
<br>
<% if @graph6 ==  "La BD de productos o la BD de WebProductos está vacía"%>
  La BD de productos o la BD de WebProductos está vacía
  <% else %>
  <%= image_tag @graph6 %>
  <% end %>

</center>
  <tbody>
    <% @dashboards.each do |dashboard| %>
      <tr>
        <td><%= link_to 'Show', dashboard %></td>
        <td><%= link_to 'Edit', edit_dashboard_path(dashboard) %></td>
        <td><%= link_to 'Destroy', dashboard, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<!-- <%= link_to 'New Dashboard', new_dashboard_path %> -->
