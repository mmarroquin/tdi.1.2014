<%= form_for(@gdoc) do |f| %>
  <% if @gdoc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gdoc.errors.count, "error") %> prohibited this gdoc from being saved:</h2>

      <ul>
      <% @gdoc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
