<%= form_for(@e_commerce) do |f| %>
  <% if @e_commerce.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@e_commerce.errors.count, "error") %> prohibited this e_commerce from being saved:</h2>

      <ul>
      <% @e_commerce.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
